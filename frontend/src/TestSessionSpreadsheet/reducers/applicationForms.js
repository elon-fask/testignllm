export const applicationTypesDefaultState = {};
export const applicationTypeIDsDefaultState = [];

export const ApplicationTypeIDsReducer = (state = applicationTypeIDsDefaultState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export const ApplicationTypesReducer = (state = applicationTypesDefaultState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};
